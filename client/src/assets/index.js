import { Images } from "./images";
import { IconsA } from "./icons";

const ImageS = {
  ProfileImage: Images.profile, // Akses gambar dari objek Images
  IconsA: IconsA, // Simpan semua ikon
  background: Images.background,
};

export { ImageS };
